import React from 'react';
import style from '../../css/SortBy.module.css';

const SortBy = ({}) => {
    return (
      <div className={style.wrapper}>
          <input type="text"/>
      </div>
    );
};

export default SortBy;